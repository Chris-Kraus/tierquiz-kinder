// Ergebnis-/Abschluss-Bildschirm: Punktestand, "Nochmal spielen" / "Zurück zum Start"
// (siehe design.md). Platzhalter-Stub für dieses Grundgerüst-Issue (#3) – fachliche
// Umsetzung folgt in einer eigenen Story.

export function renderResultScreen(/* container, result */) {
  throw new Error("renderResultScreen: noch nicht implementiert");
}
