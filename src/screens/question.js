// Frage-Bildschirm: Fragetext, 4 Antwortkacheln, Sofort-Feedback (siehe design.md).
// Platzhalter-Stub für dieses Grundgerüst-Issue (#3) – fachliche Umsetzung folgt
// in einer eigenen Story.

export function renderQuestionScreen(/* container, question */) {
  throw new Error("renderQuestionScreen: noch nicht implementiert");
}
